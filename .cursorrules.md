You are implementing a software project called Noctua by following a phased 
implementation plan. The full plan is in `docs/implementation_plan.md`. 
The complete codebase documentation is in `docs/codebase_snapshot.md`.

Before writing any code, follow this process exactly:

STEP 1 — READ FIRST, CODE SECOND
Read the full content of the phase(s) I specify below before touching any file.
Read the relevant sections of `docs/codebase_snapshot.md` for every file you 
will modify — understand what already exists before adding to it.
Do not begin implementation until you have confirmed your understanding.

STEP 2 — DECLARE YOUR PLAN
Before writing any code, tell me:
- Which files you will create (new files)
- Which files you will modify (existing files) and what specifically will change
- Any assumptions you are making that the plan does not explicitly resolve
- Any conflicts you see between the plan and the existing code

Wait for my confirmation before proceeding.

STEP 3 — IMPLEMENT IN ORDER
Work through the phase sections in the order they are written.
Complete each section fully before moving to the next.
Do not skip ahead.

STEP 4 — FOLLOW EXISTING CONVENTIONS
Match the code style of the files you are modifying exactly:
- Python: match import ordering, typing conventions, docstring style, 
  error handling patterns visible in the existing service files
- TypeScript: match the component patterns, hook conventions, and 
  axios call style already present in api.ts and BookContext.tsx
- Tests: match the fixture approach, parametrize style, and assertion 
  conventions of the existing test files in backend/tests/

STEP 5 — WRITE TESTS AS YOU GO
Write the unit tests specified in the phase at the same time as the 
implementation — not after. Tests and implementation ship together.

STEP 6 — REPORT COMPLETION
When the phase is complete, tell me:
- Every file created or modified (with the change summary)
- Any tests that are skipped and why
- Any deviations from the plan and why you made them
- What to run to verify the implementation works

CONSTRAINTS — enforce these without exception:
- Never modify files outside the "Files touched" list for the current phase 
  without flagging it to me first
- Never delete existing functionality to make room for new functionality — 
  extend, don't replace, unless the plan explicitly says to replace
- If the plan specifies a function signature, use it exactly — do not 
  "improve" the interface without asking
- If you are uncertain whether something in the plan conflicts with the 
  existing codebase, stop and ask rather than guessing
- Do not add dependencies (pip packages, npm packages) beyond those 
  specified in the plan without asking first

---

CURRENT SESSION: implement the following phase(s) from the plan:

[PASTE THE PHASE CONTENT HERE]